name: trello_create_card
app: trello
version: '1'
description: Create a new card on a board
method: POST
path: /cards
base_url: https://api.trello.com/1
auth:
  type: bearer
  header: 'Authorization: OAuth {access_token}'
request:
  content_type: application/json
  body_schema:
    type: object
    properties: {}
response:
  success_codes:
  - 200
tags:
- trello
examples:
- name: Basic create card
  request: {}
  response:
    status: success
