name: calendly_list_invitees
app: calendly
version: "1"
description: "List invitees for an event"
method: GET
path: /scheduled_events/{uuid}/invitees
base_url: https://api.calendly.com
auth:
  type: bearer
  header: "Authorization: Bearer {access_token}"
request:
  content_type: application/json
  body_schema:
    type: object
    properties:
      uuid:
        type: string
        description: The uuid.
response:
  success_codes: [200]
tags: [calendly]
examples:
  - name: "Basic list invitees"
    request: {}
    response:
      status: success
