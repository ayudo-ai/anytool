# ──────────────────────────────────────────────────────────────────────
# Google Sheets — Sheets Create Spreadsheet
# Tier 1: Auto-generated from Google Discovery doc.
# Review and adjust descriptions/examples before use.
# ──────────────────────────────────────────────────────────────────────

name: sheets_create_spreadsheet
app: google
version: '1'
description: Creates a spreadsheet, returning the newly created spreadsheet.
method: POST
path: /v4/spreadsheets
base_url: https://sheets.googleapis.com
auth:
  type: oauth2
  scopes:
  - https://www.googleapis.com/auth/spreadsheets
  header: 'Authorization: Bearer {access_token}'
request:
  content_type: application/json
  body_schema:
    type: object
    properties:
      properties:
        type: object
        properties:
          title:
            type: string
            description: The title of the spreadsheet.
          locale:
            type: string
            description: 'The locale of the spreadsheet in one of the following formats: * an ISO 639-1 language code such
              as `en` * an ISO 639-2 language code such as `fil`, if no 639-1 code exists * a combination of the ISO language
              code and country code, such as `en_US` Note: when updating this field, not all locales/languages are supported.'
          autoRecalc:
            type: string
            enum:
            - RECALCULATION_INTERVAL_UNSPECIFIED
            - ON_CHANGE
            - MINUTE
            - HOUR
            description: The amount of time to wait before volatile functions are recalculated.
          timeZone:
            type: string
            description: The time zone of the spreadsheet, in CLDR format such as `America/New_York`. If the time zone isn't
              recognized, this may be a custom time zone such as `GMT-07:00`.
          defaultFormat:
            type: object
            properties:
              numberFormat:
                type: object
                description: Nested object (depth limit reached)
              backgroundColor:
                type: object
                description: Nested object (depth limit reached)
              backgroundColorStyle:
                type: object
                description: Nested object (depth limit reached)
              borders:
                type: object
                description: Nested object (depth limit reached)
              padding:
                type: object
                description: Nested object (depth limit reached)
              horizontalAlignment:
                type: string
                enum:
                - HORIZONTAL_ALIGN_UNSPECIFIED
                - LEFT
                - CENTER
                - RIGHT
                description: The horizontal alignment of the value in the cell.
              verticalAlignment:
                type: string
                enum:
                - VERTICAL_ALIGN_UNSPECIFIED
                - TOP
                - MIDDLE
                - BOTTOM
                description: The vertical alignment of the value in the cell.
              wrapStrategy:
                type: string
                enum:
                - WRAP_STRATEGY_UNSPECIFIED
                - OVERFLOW_CELL
                - LEGACY_WRAP
                - CLIP
                - WRAP
                description: The wrap strategy for the value in the cell.
              textDirection:
                type: string
                enum:
                - TEXT_DIRECTION_UNSPECIFIED
                - LEFT_TO_RIGHT
                - RIGHT_TO_LEFT
                description: The direction of the text in the cell.
              textFormat:
                type: object
                description: Nested object (depth limit reached)
              hyperlinkDisplayType:
                type: string
                enum:
                - HYPERLINK_DISPLAY_TYPE_UNSPECIFIED
                - LINKED
                - PLAIN_TEXT
                description: If one exists, how a hyperlink should be displayed in the cell.
              textRotation:
                type: object
                description: Nested object (depth limit reached)
            description: The format of a cell.
          iterativeCalculationSettings:
            type: object
            properties:
              maxIterations:
                type: integer
                format: int32
                description: When iterative calculation is enabled, the maximum number of calculation rounds to perform.
              convergenceThreshold:
                type: number
                description: When iterative calculation is enabled and successive results differ by less than this threshold
                  value, the calculation rounds stop.
            description: Settings to control how circular dependencies are resolved with iterative calculation.
          spreadsheetTheme:
            type: object
            properties:
              primaryFontFamily:
                type: string
                description: Name of the primary font family.
              themeColors:
                type: array
                items:
                  type: object
                  description: Nested object (depth limit reached)
                description: The spreadsheet theme color pairs. To update you must provide all theme color pairs.
            description: Represents spreadsheet theme
          importFunctionsExternalUrlAccessAllowed:
            type: boolean
            description: Whether to allow external URL access for image and import functions. Read only when true. When false,
              you can set to true. This value will be bypassed and always return true if the admin has enabled the [allowlisting
              feature](https://support.google.com/a?p=url_allowlist).
        description: Properties of a spreadsheet.
      sheets:
        type: array
        items:
          type: object
          properties:
            properties:
              type: object
              properties:
                sheetId:
                  type: object
                  description: Nested object (depth limit reached)
                title:
                  type: object
                  description: Nested object (depth limit reached)
                index:
                  type: object
                  description: Nested object (depth limit reached)
                sheetType:
                  type: object
                  description: Nested object (depth limit reached)
                gridProperties:
                  type: object
                  description: Nested object (depth limit reached)
                hidden:
                  type: object
                  description: Nested object (depth limit reached)
                tabColor:
                  type: object
                  description: Nested object (depth limit reached)
                tabColorStyle:
                  type: object
                  description: Nested object (depth limit reached)
                rightToLeft:
                  type: object
                  description: Nested object (depth limit reached)
                dataSourceSheetProperties:
                  type: object
                  description: Nested object (depth limit reached)
              description: Properties of a sheet.
            data:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: Data in the grid, if this is a grid sheet. The number of GridData objects returned is dependent
                on the number of ranges requested on this sheet. For example, if this is representing `Sheet1`, and the spreadsheet
                was requested with ranges `Sheet1!A1:C10` and `Sheet1!D15:E20`, then the first GridData will have a startRow/startColumn
                of `0`, while the second one will have `startRow 14` (zero-based row 15), and `startColumn 3` (zero-based
                column D). For a DATA_SOURCE sheet, you can not request a specific range, the GridData contains all the values.
            merges:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The ranges that are merged together.
            conditionalFormats:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The conditional format rules in this sheet.
            filterViews:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The filter views in this sheet.
            protectedRanges:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The protected ranges in this sheet.
            basicFilter:
              type: object
              properties:
                range:
                  type: object
                  description: Nested object (depth limit reached)
                tableId:
                  type: object
                  description: Nested object (depth limit reached)
                sortSpecs:
                  type: object
                  description: Nested object (depth limit reached)
                criteria:
                  type: object
                  description: Nested object (depth limit reached)
                filterSpecs:
                  type: object
                  description: Nested object (depth limit reached)
              description: The default filter associated with a sheet. For more information, see [Manage data visibility with
                filters](https://developers.google.com/workspace/sheets/api/guides/filters).
            charts:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The specifications of every chart on this sheet.
            bandedRanges:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The banded (alternating colors) ranges on this sheet.
            developerMetadata:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The developer metadata associated with a sheet.
            rowGroups:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: All row groups on this sheet, ordered by increasing range start index, then by group depth.
            columnGroups:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: All column groups on this sheet, ordered by increasing range start index, then by group depth.
            slicers:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The slicers on this sheet.
            tables:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The tables on this sheet.
          description: A sheet in a spreadsheet.
        description: The sheets that are part of a spreadsheet.
      namedRanges:
        type: array
        items:
          type: object
          properties:
            namedRangeId:
              type: string
              description: The ID of the named range.
            name:
              type: string
              description: The name of the named range.
            range:
              type: object
              properties:
                sheetId:
                  type: object
                  description: Nested object (depth limit reached)
                startRowIndex:
                  type: object
                  description: Nested object (depth limit reached)
                endRowIndex:
                  type: object
                  description: Nested object (depth limit reached)
                startColumnIndex:
                  type: object
                  description: Nested object (depth limit reached)
                endColumnIndex:
                  type: object
                  description: Nested object (depth limit reached)
              description: 'A range on a sheet. All indexes are zero-based. Indexes are half open, i.e. the start index is
                inclusive and the end index is exclusive -- [start_index, end_index). Missing indexes indicate the range is
                unbounded on that side. For example, if `"Sheet1"` is sheet ID 123456, then: `Sheet1!A1:A1 == sheet_id: 123456,
                start_row_index: 0, end_row_index: 1, start_column_index: 0, end_column_index: 1` `Sheet1!A3:B4 == sheet_id:
                123456, start_row_index: 2, end_row_index: 4, start_column_index: 0, end_column_index: 2` `Sheet1!A:B == sheet_id:
                123456, start_column_index: 0, end_column_index: 2` `Sheet1!A5:B == sheet_id: 123456, start_row_index: 4,
                start_column_index: 0, end_column_index: 2` `Sheet1 == sheet_id: 123456` The start index must always be less
                than or equal to the end index. If the start index equals the end index, then the range is empty. Empty ranges
                are typically not meaningful and are usually rendered in the UI as `#REF!`.'
          description: A named range.
        description: The named ranges defined in a spreadsheet.
      developerMetadata:
        type: array
        items:
          type: object
          properties:
            metadataId:
              type: integer
              format: int32
              description: The spreadsheet-scoped unique ID that identifies the metadata. IDs may be specified when metadata
                is created, otherwise one will be randomly generated and assigned. Must be positive.
            metadataKey:
              type: string
              description: The metadata key. There may be multiple metadata in a spreadsheet with the same key. Developer
                metadata must always have a key specified.
            metadataValue:
              type: string
              description: Data associated with the metadata's key.
            location:
              type: object
              properties:
                locationType:
                  type: object
                  description: Nested object (depth limit reached)
                spreadsheet:
                  type: object
                  description: Nested object (depth limit reached)
                sheetId:
                  type: object
                  description: Nested object (depth limit reached)
                dimensionRange:
                  type: object
                  description: Nested object (depth limit reached)
              description: A location where metadata may be associated in a spreadsheet.
            visibility:
              type: string
              enum:
              - DEVELOPER_METADATA_VISIBILITY_UNSPECIFIED
              - DOCUMENT
              - PROJECT
              description: The metadata visibility. Developer metadata must always have visibility specified.
          description: Developer metadata associated with a location or object in a spreadsheet. For more information, see
            [Read, write, and search metadata](https://developers.google.com/workspace/sheets/api/guides/metadata). Developer
            metadata may be used to associate arbitrary data with various parts of a spreadsheet and it will remain associated
            at those locations as they move around and the spreadsheet is edited. For example, if developer metadata is associated
            with row 5 and another row is then subsequently inserted above row 5, that original metadata is still associated
            with the row it was first associated with (what is now row 6). If the associated object is deleted then its metadata
            is deleted too.
        description: The developer metadata associated with a spreadsheet.
      dataSources:
        type: array
        items:
          type: object
          properties:
            dataSourceId:
              type: string
              description: 'The spreadsheet-scoped unique ID that identifies the data source. Example: 1080547365.'
            spec:
              type: object
              properties:
                bigQuery:
                  type: object
                  description: Nested object (depth limit reached)
                looker:
                  type: object
                  description: Nested object (depth limit reached)
                parameters:
                  type: object
                  description: Nested object (depth limit reached)
              description: This specifies the details of the data source. For example, for BigQuery, this specifies information
                about the BigQuery source.
            calculatedColumns:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: All calculated columns in the data source.
            sheetId:
              type: integer
              format: int32
              description: The ID of the Sheet connected with the data source. The field cannot be changed once set. When
                creating a data source, an associated DATA_SOURCE sheet is also created, if the field is not specified, the
                ID of the created sheet will be randomly generated.
          description: Information about an external data source in the spreadsheet.
        description: A list of external data sources connected with the spreadsheet.
      dataSourceSchedules:
        type: array
        items:
          type: object
          properties:
            enabled:
              type: boolean
              description: True if the refresh schedule is enabled, or false otherwise.
            refreshScope:
              type: string
              enum:
              - DATA_SOURCE_REFRESH_SCOPE_UNSPECIFIED
              - ALL_DATA_SOURCES
              description: The scope of the refresh. Must be ALL_DATA_SOURCES.
            dailySchedule:
              type: object
              properties:
                startTime:
                  type: object
                  description: Nested object (depth limit reached)
              description: A schedule for data to refresh every day in a given time interval.
            weeklySchedule:
              type: object
              properties:
                startTime:
                  type: object
                  description: Nested object (depth limit reached)
                daysOfWeek:
                  type: object
                  description: Nested object (depth limit reached)
              description: A weekly schedule for data to refresh on specific days in a given time interval.
            monthlySchedule:
              type: object
              properties:
                startTime:
                  type: object
                  description: Nested object (depth limit reached)
                daysOfMonth:
                  type: object
                  description: Nested object (depth limit reached)
              description: A monthly schedule for data to refresh on specific days in the month in a given time interval.
            nextRun:
              type: object
              properties:
                startTime:
                  type: object
                  description: Nested object (depth limit reached)
                endTime:
                  type: object
                  description: Nested object (depth limit reached)
              description: Represents a time interval, encoded as a Timestamp start (inclusive) and a Timestamp end (exclusive).
                The start must be less than or equal to the end. When the start equals the end, the interval is empty (matches
                no time). When both start and end are unspecified, the interval matches any time.
          description: Schedule for refreshing the data source. Data sources in the spreadsheet are refreshed within a time
            interval. You can specify the start time by clicking the Scheduled Refresh button in the Sheets editor, but the
            interval is fixed at 4 hours. For example, if you specify a start time of 8 AM , the refresh will take place between
            8 AM and 12 PM every day.
        description: Output only. A list of data source refresh schedules.
    description: Resource that represents a spreadsheet.
response:
  success_codes:
  - 200
  body_schema:
    type: object
    properties:
      spreadsheetId:
        type: string
        description: The ID of the spreadsheet. This field is read-only.
      properties:
        type: object
        properties:
          title:
            type: string
            description: The title of the spreadsheet.
        description: Properties of a spreadsheet.
      sheets:
        type: array
        items:
          type: object
          properties:
            properties:
              type: object
              properties:
                sheetId:
                  type: object
                  description: Nested object (depth limit reached)
                title:
                  type: object
                  description: Nested object (depth limit reached)
                index:
                  type: object
                  description: Nested object (depth limit reached)
                sheetType:
                  type: object
                  description: Nested object (depth limit reached)
                gridProperties:
                  type: object
                  description: Nested object (depth limit reached)
                hidden:
                  type: object
                  description: Nested object (depth limit reached)
                tabColor:
                  type: object
                  description: Nested object (depth limit reached)
                tabColorStyle:
                  type: object
                  description: Nested object (depth limit reached)
                rightToLeft:
                  type: object
                  description: Nested object (depth limit reached)
                dataSourceSheetProperties:
                  type: object
                  description: Nested object (depth limit reached)
              description: Properties of a sheet.
            data:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: Data in the grid, if this is a grid sheet. The number of GridData objects returned is dependent
                on the number of ranges requested on this sheet. For example, if this is representing `Sheet1`, and the spreadsheet
                was requested with ranges `Sheet1!A1:C10` and `Sheet1!D15:E20`, then the first GridData will have a startRow/startColumn
                of `0`, while the second one will have `startRow 14` (zero-based row 15), and `startColumn 3` (zero-based
                column D). For a DATA_SOURCE sheet, you can not request a specific range, the GridData contains all the values.
            merges:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The ranges that are merged together.
            conditionalFormats:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The conditional format rules in this sheet.
            filterViews:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The filter views in this sheet.
            protectedRanges:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The protected ranges in this sheet.
            basicFilter:
              type: object
              properties:
                range:
                  type: object
                  description: Nested object (depth limit reached)
                tableId:
                  type: object
                  description: Nested object (depth limit reached)
                sortSpecs:
                  type: object
                  description: Nested object (depth limit reached)
                criteria:
                  type: object
                  description: Nested object (depth limit reached)
                filterSpecs:
                  type: object
                  description: Nested object (depth limit reached)
              description: The default filter associated with a sheet. For more information, see [Manage data visibility with
                filters](https://developers.google.com/workspace/sheets/api/guides/filters).
            charts:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The specifications of every chart on this sheet.
            bandedRanges:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The banded (alternating colors) ranges on this sheet.
            developerMetadata:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The developer metadata associated with a sheet.
            rowGroups:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: All row groups on this sheet, ordered by increasing range start index, then by group depth.
            columnGroups:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: All column groups on this sheet, ordered by increasing range start index, then by group depth.
            slicers:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The slicers on this sheet.
            tables:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The tables on this sheet.
          description: A sheet in a spreadsheet.
        description: The sheets that are part of a spreadsheet.
      spreadsheetUrl:
        type: string
        description: The url of the spreadsheet. This field is read-only.
    description: Resource that represents a spreadsheet.
tags:
- google
- sheets
examples:
- name: Basic sheets create spreadsheet
  request: {}
  response:
    status: success
