# ──────────────────────────────────────────────────────────────────────
# Google Calendar — Calendar List Calendars
# Tier 1: Auto-generated from Google Discovery doc.
# Review and adjust descriptions/examples before use.
# ──────────────────────────────────────────────────────────────────────

name: calendar_list_calendars
app: google
version: '1'
description: Returns the calendars on the user's calendar list.
method: GET
path: /users/me/calendarList
base_url: https://www.googleapis.com
auth:
  type: oauth2
  scopes:
  - https://www.googleapis.com/auth/calendar
  header: 'Authorization: Bearer {access_token}'
request:
  query_params:
    maxResults:
      type: integer
      description: Maximum number of entries returned on one result page. By default the value is 100 entries. The page size
        can never be larger than 250 entries. Optional.
    minAccessRole:
      type: string
      description: The minimum access role for the user in the returned entries. Optional. The default is no restriction.
      enum:
      - freeBusyReader
      - owner
      - reader
      - writer
    pageToken:
      type: string
      description: Token specifying which result page to return. Optional.
    showDeleted:
      type: boolean
      description: Whether to include deleted calendar list entries in the result. Optional. The default is False.
    showHidden:
      type: boolean
      description: Whether to show hidden entries. Optional. The default is False.
    syncToken:
      type: string
      description: 'Token obtained from the nextSyncToken field returned on the last page of results from the previous list
        request. It makes the result of this list request contain only entries that have changed since then. If only read-only
        fields such as calendar properties or ACLs have changed, the entry won''t be returned. All entries deleted and hidden
        since the previous list request will always be in the result set and it is not allowed to set showDeleted neither
        showHidden to False.

        To ensure client state consistency minAccessRole query parameter cannot be specified together with nextSyncToken.

        If the syncToken expires, the server will respond with a 410 GONE response code and the client should clear its storage
        and perform a full synchronization without any syncToken.

        Learn more about incremental synchronization.

        Optional. The default is to return all entries.'
response:
  success_codes:
  - 200
  body_schema:
    type: object
    properties:
      items:
        type: array
        items:
          type: object
          properties:
            accessRole:
              type: string
              description: "The effective access role that the authenticated user has on the calendar. Read-only. Possible\
                \ values are:  \n- \"freeBusyReader\" - Provides read access to free/busy information. \n- \"reader\" - Provides\
                \ read access to the calendar. Private events will appear to users with reader access, but event details will\
                \ be hidden. \n- \"writer\" - Provides read and write access to the calendar. Private events will appear to\
                \ users with writer access, and event details will be visible. \n- \"owner\" - Provides manager access to\
                \ the calendar. This role has all of the permissions of the writer role with the additional ability to see\
                \ and modify access levels of other users.\nImportant: the owner role is different from the calendar's data\
                \ owner. A calendar has a single data owner, but can have multiple users with owner role."
            autoAcceptInvitations:
              type: boolean
              description: Whether this calendar automatically accepts invitations. Only valid for resource calendars. Read-only.
            backgroundColor:
              type: string
              description: The main color of the calendar in the hexadecimal format "#0088aa". This property supersedes the
                index-based colorId property. To set or change this property, you need to specify colorRgbFormat=true in the
                parameters of the insert, update and patch methods. Optional.
            colorId:
              type: string
              description: The color of the calendar. This is an ID referring to an entry in the calendar section of the colors
                definition (see the colors endpoint). This property is superseded by the backgroundColor and foregroundColor
                properties and can be ignored when using these properties. Optional.
            conferenceProperties:
              type: object
              properties:
                allowedConferenceSolutionTypes:
                  type: object
                  description: Nested object (depth limit reached)
            dataOwner:
              type: string
              description: The email of the owner of the calendar. Set only for secondary calendars. Read-only.
            defaultReminders:
              type: array
              items:
                type: object
                description: Nested object (depth limit reached)
              description: The default reminders that the authenticated user has for this calendar.
            deleted:
              type: boolean
              description: Whether this calendar list entry has been deleted from the calendar list. Read-only. Optional.
                The default is False.
              default: 'false'
            description:
              type: string
              description: Description of the calendar. Optional. Read-only.
            etag:
              type: string
              description: ETag of the resource.
            foregroundColor:
              type: string
              description: The foreground color of the calendar in the hexadecimal format "#ffffff". This property supersedes
                the index-based colorId property. To set or change this property, you need to specify colorRgbFormat=true
                in the parameters of the insert, update and patch methods. Optional.
            hidden:
              type: boolean
              description: Whether the calendar has been hidden from the list. Optional. The attribute is only returned when
                the calendar is hidden, in which case the value is true.
              default: 'false'
            id:
              type: string
              description: Identifier of the calendar.
            kind:
              type: string
              description: Type of the resource ("calendar#calendarListEntry").
              default: calendar#calendarListEntry
            location:
              type: string
              description: Geographic location of the calendar as free-form text. Optional. Read-only.
            notificationSettings:
              type: object
              properties:
                notifications:
                  type: array
                  items:
                    type: object
                    description: Nested object (depth limit reached)
                  description: The list of notifications set for this calendar.
              description: The notifications that the authenticated user is receiving for this calendar.
            primary:
              type: boolean
              description: Whether the calendar is the primary calendar of the authenticated user. Read-only. Optional. The
                default is False.
              default: 'false'
            selected:
              type: boolean
              description: Whether the calendar content shows up in the calendar UI. Optional. The default is False.
              default: 'false'
            summary:
              type: string
              description: Title of the calendar. Read-only.
            summaryOverride:
              type: string
              description: The summary that the authenticated user has set for this calendar. Optional.
            timeZone:
              type: string
              description: The time zone of the calendar. Optional. Read-only.
        description: Calendars that are present on the user's calendar list.
      kind:
        type: string
        description: Type of the collection ("calendar#calendarList").
        default: calendar#calendarList
      nextPageToken:
        type: string
        description: Token used to access the next page of this result. Omitted if no further results are available, in which
          case nextSyncToken is provided.
tags:
- google
- calendar
examples:
- name: Basic calendar list calendars
  request: {}
  response:
    status: success
