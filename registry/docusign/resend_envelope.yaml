# Auto-migrated from old spec format
# Review and enhance: add examples, refine body_schema nesting

name: docusign_resend_envelope
app: docusign
description: Resend notifications for a sent envelope. Sends a new email to recipients who haven't completed signing.
method: PUT
path: /restapi/v2.1/accounts/{account_id}/envelopes/{envelope_id}/recipients
base_url: https://demo.docusign.net
auth:
  type: oauth2
  header: 'Authorization: Bearer {access_token}'
  inject_from_metadata:
    account_id: account_id
request:
  content_type: application/json
  note: 'Note: Old spec used transform ''docusign_resend''. In v2, the LLM constructs the exact body structure directly.'
  body_schema:
    type: object
    properties:
      envelope_id:
        type: string
        description: Envelope ID to resend
    required:
    - envelope_id
response:
  success_codes:
  - 200
  - 201
tags:
- esignature
- documents
