# ──────────────────────────────────────────────────────────────────────
# Jira — Jira Deleteissue
# Auto-generated from OpenAPI spec. Review before use.
# ──────────────────────────────────────────────────────────────────────
name: jira_delete_issue
app: jira
version: '1'
description: "Deletes an issue.\n\nAn issue cannot be deleted if it has one or more subtasks. To delete an issue with subtasks,\
  \ set `deleteSubtasks`. This causes the issue's subtasks to be deleted with the issue.\n\nThis operation can be accessed\
  \ anonymously.\n\n**[Permissions](#permissions) required:**\n\n *  *Browse projects* and *Delete issues* [project permission](https://confluence.atlassian.com/x/yodKLg)\
  \ for the project containing the issue.\n *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured,\
  \ issue-level security permission to view the issue."
method: DELETE
path: /rest/api/3/issue/{issueIdOrKey}
base_url: https://{domain}.atlassian.net
auth:
  type: oauth2
  header: 'Authorization: Bearer {access_token}'
request:
  query_params:
    deleteSubtasks:
      type: string
      enum:
      - 'true'
      - 'false'
      default: 'false'
      description: Whether the issue's subtasks are deleted when the issue is deleted.
  body_schema:
    type: object
    properties:
      issueIdOrKey:
        type: string
        description: The issueIdOrKey.
  content_type: application/json
response:
  success_codes:
  - 200
  - 204
tags:
- Issues
