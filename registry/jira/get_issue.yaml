# ──────────────────────────────────────────────────────────────────────
# Jira — Jira Getissue
# Auto-generated from OpenAPI spec. Review before use.
# ──────────────────────────────────────────────────────────────────────
name: jira_get_issue
app: jira
version: '1'
description: "Returns the details for an issue.\n\nThe issue is identified by its ID or key, however, if the identifier doesn't\
  \ match an issue, a case-insensitive search and check for moved issues is performed. If a matching issue is found its details\
  \ are returned, a 302 or other redirect is **not** returned. The issue key returned in the response is the key of the issue\
  \ found.\n\nThis operation can be accessed anonymously.\n\n**[Permissions](#permissions) required:**\n\n *  *Browse projects*\
  \ [project permission](https://confluence.atlassian.com/x/yodKLg) for the project that the issue is in.\n *  If [issue-level\
  \ security](https://confluence.atlassian.com/x/J4lKLg) is configured, issue-level security permission to view the issue."
method: GET
path: /rest/api/3/issue/{issueIdOrKey}
base_url: https://{domain}.atlassian.net
auth:
  type: oauth2
  header: 'Authorization: Bearer {access_token}'
request:
  query_params:
    fields:
      type: array
      items:
        type: string
        default: '*all'
      description: "A list of fields to return for the issue. This parameter accepts a comma-separated list. Use it to retrieve\
        \ a subset of fields. Allowed values:\n\n *  `*all` Returns all fields.\n *  `*navigable` Returns navigable fields.\n\
        \ *  Any issue field, prefixed with a minus to exclude.\n\nExamples:\n\n *  `summary,comment` Returns only the summary\
        \ and comments fields.\n *  `-description` Returns all (default) fields except description.\n *  `*navigable,-comment`\
        \ Returns all navigable fields except comment.\n\nThis parameter may be specified multiple times. For example, `fields=field1,field2&\
        \ fields=field3`.\n\nNote: All fields are returned by default. This differs from [Search for issues using JQL (GET)](#api-rest-api-3-search-get)\
        \ and [Search for issues using JQL (POST)](#api-rest-api-3-search-post) where the default is all navigable fields."
    fieldsByKeys:
      type: boolean
      default: false
      description: Whether fields in `fields` are referenced by keys rather than IDs. This parameter is useful where fields
        have been added by a connect app and a field's key may differ from its ID.
    expand:
      type: string
      description: "Use [expand](#expansion) to include additional information about the issues in the response. This parameter\
        \ accepts a comma-separated list. Expand options include:\n\n *  `renderedFields` Returns field values rendered in\
        \ HTML format.\n *  `names` Returns the display name of each field.\n *  `schema` Returns the schema describing a\
        \ field type.\n *  `transitions` Returns all possible transitions for the issue.\n *  `editmeta` Returns information\
        \ about how each field can be edited.\n *  `changelog` Returns a list of recent updates to an issue, sorted by date,\
        \ starting from the most recent.\n *  `versionedRepresentations` Returns a JSON array for each version of a field's\
        \ value, with the highest number representing the most recent version. Note: When included in the request, the `fields`\
        \ parameter is ignored."
    properties:
      type: array
      items:
        type: string
        default: 'null'
      description: "A list of issue properties to return for the issue. This parameter accepts a comma-separated list. Allowed\
        \ values:\n\n *  `*all` Returns all issue properties.\n *  Any issue property key, prefixed with a minus to exclude.\n\
        \nExamples:\n\n *  `*all` Returns all properties.\n *  `*all,-prop1` Returns all properties except `prop1`.\n *  `prop1,prop2`\
        \ Returns `prop1` and `prop2` properties.\n\nThis parameter may be specified multiple times. For example, `properties=prop1,prop2&\
        \ properties=prop3`."
    updateHistory:
      type: boolean
      default: false
      description: Whether the project in which the issue is created is added to the user's **Recently viewed** project list,
        as shown under **Projects** in Jira. This also populates the [JQL issues search](#api-rest-api-3-search-get) `lastViewed`
        field.
    failFast:
      type: boolean
      default: false
      description: Whether to fail the request quickly in case of an error while loading fields for an issue. For `failFast=true`,
        if one field fails, the entire operation fails. For `failFast=false`, the operation will continue even if a field
        fails. It will return a valid response, but without values for the failed field(s).
  body_schema:
    type: object
    properties:
      issueIdOrKey:
        type: string
        description: The issueIdOrKey.
  content_type: application/json
response:
  success_codes:
  - 200
  body_schema:
    type: object
    description: Details about an issue.
    properties:
      changelog:
        type: object
        properties:
          histories:
            type: array
            description: The list of changelogs.
            items:
              type: object
          maxResults:
            type: integer
            description: The maximum number of results that could be on the page.
          startAt:
            type: integer
            description: The index of the first item returned on the page.
          total:
            type: integer
            description: The number of results on the page.
      editmeta:
        type: object
        properties:
          fields:
            type: object
      expand:
        type: string
        description: Expand options that include additional issue details in the response.
      fields:
        type: object
      fieldsToInclude:
        type: object
        properties:
          actuallyIncluded:
            type: array
            items:
              type: string
          excluded:
            type: array
            items:
              type: string
          included:
            type: array
            items:
              type: string
      id:
        type: string
        description: The ID of the issue.
      key:
        type: string
        description: The key of the issue.
      names:
        type: object
        description: The ID and name of each field present on the issue.
      operations:
        type: object
        properties:
          linkGroups:
            type: array
            description: Details of the link groups defining issue operations.
            items:
              type: object
      properties:
        type: object
        description: Details of the issue properties identified in the request.
      renderedFields:
        type: object
        description: The rendered value of each field present on the issue.
      schema:
        type: object
        description: The schema describing each field present on the issue.
      self:
        type: string
        description: The URL of the issue details.
      transitions:
        type: array
        description: The transitions that can be performed on the issue.
        items:
          type: object
          description: Details of an issue transition.
          properties:
            expand:
              type: string
              description: Expand options that include additional transition details in the response.
            fields:
              type: object
              description: Details of the fields associated with the issue transition screen. Use this information to populate
                `fields` and `update` in a transition request.
            hasScreen:
              type: boolean
              description: Whether there is a screen associated with the issue transition.
            id:
              type: string
              description: The ID of the issue transition. Required when specifying a transition to undertake.
            isAvailable:
              type: boolean
              description: Whether the transition is available to be performed.
            isConditional:
              type: boolean
              description: Whether the issue has to meet criteria before the issue transition is applied.
            isGlobal:
              type: boolean
              description: Whether the issue transition is global, that is, the transition is applied to issues regardless
                of their status.
            isInitial:
              type: boolean
              description: Whether this is the initial issue transition for the workflow.
            looped:
              type: boolean
            name:
              type: string
              description: The name of the issue transition.
            to:
              type: object
              properties: {}
      versionedRepresentations:
        type: object
        description: The versions of each field on the issue.
tags:
- Issues
