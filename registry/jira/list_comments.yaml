# ──────────────────────────────────────────────────────────────────────
# Jira — Jira Getcomments
# Auto-generated from OpenAPI spec. Review before use.
# ──────────────────────────────────────────────────────────────────────
name: jira_list_comments
app: jira
version: '1'
description: "Returns all comments for an issue.\n\nThis operation can be accessed anonymously.\n\n**[Permissions](#permissions)\
  \ required:** Comments are included in the response where the user has:\n\n *  *Browse projects* [project permission](https://confluence.atlassian.com/x/yodKLg)\
  \ for the project containing the comment.\n *  If [issue-level security](https://confluence.atlassian.com/x/J4lKLg) is configured,\
  \ issue-level security permission to view the issue.\n *  If the comment has visibility restrictions, belongs to the group\
  \ or has the role visibility is role visibility is restricted to."
method: GET
path: /rest/api/3/issue/{issueIdOrKey}/comment
base_url: https://{domain}.atlassian.net
auth:
  type: oauth2
  header: 'Authorization: Bearer {access_token}'
request:
  query_params:
    startAt:
      type: integer
      default: 0
      description: The index of the first item to return in a page of results (page offset).
    maxResults:
      type: integer
      default: 100
      description: The maximum number of items to return per page.
    orderBy:
      type: string
      enum:
      - created
      - -created
      - +created
      description: '[Order](#ordering) the results by a field. Accepts *created* to sort comments by their created date.'
    expand:
      type: string
      description: Use [expand](#expansion) to include additional information about comments in the response. This parameter
        accepts `renderedBody`, which returns the comment body rendered in HTML.
  body_schema:
    type: object
    properties:
      issueIdOrKey:
        type: string
        description: The issueIdOrKey.
  content_type: application/json
response:
  success_codes:
  - 200
  body_schema:
    type: object
    description: A page of comments.
    properties:
      comments:
        type: array
        description: The list of comments.
        items:
          type: object
          description: A comment.
          properties:
            author:
              type: object
              properties: {}
            body:
              type: object
              description: The comment text in [Atlassian Document Format](https://developer.atlassian.com/cloud/jira/platform/apis/document/structure/).
            created:
              type: string
              description: The date and time at which the comment was created.
            id:
              type: string
              description: The ID of the comment.
            jsdAuthorCanSeeRequest:
              type: boolean
              description: Whether the comment was added from an email sent by a person who is not part of the issue. See
                [Allow external emails to be added as comments on issues](https://support.atlassian.com/jira-service-management-cloud/docs/allow-external-emails-to-be-added-as-comments-on-issues/)for
                information on setting up this feature.
            jsdPublic:
              type: boolean
              description: Whether the comment is visible in Jira Service Desk. Defaults to true when comments are created
                in the Jira Cloud Platform. This includes when the site doesn't use Jira Service Desk or the project isn't
                a Jira Service Desk project and, therefore, there is no Jira Service Desk for the issue to be visible on.
                To create a comment with its visibility in Jira Service Desk set to false, use the Jira Service Desk REST
                API [Create request comment](https://developer.atlassian.com/cloud/jira/service-desk/rest/#api-rest-servicedeskapi-request-issueIdOrKey-comment-post)
                operation.
            properties:
              type: array
              description: A list of comment properties. Optional on create and update.
              items:
                type: object
            renderedBody:
              type: string
              description: The rendered version of the comment.
            self:
              type: string
              description: The URL of the comment.
            updateAuthor:
              type: object
              properties: {}
            updated:
              type: string
              description: The date and time at which the comment was updated last.
            visibility:
              type: object
              properties: {}
      maxResults:
        type: integer
        description: The maximum number of items that could be returned.
      startAt:
        type: integer
        description: The index of the first item returned.
      total:
        type: integer
        description: The number of items returned.
tags:
- Issue comments
